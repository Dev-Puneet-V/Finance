import Sidebar from "./Sidebar";
import TransactionHistory from "./TransactionHistory";

const Home: React.FC = () => {
  return (
    <div>
      <Sidebar />
    </div>
  );
};

export default Home;
